/*
 * Dette grensesnittet har en metode samme sjekker om et objekt som inneholder
 * et navn (String) har samme navn som parameteren til metoden s.
 */

interface Lik {
	boolean samme(String s);
}
